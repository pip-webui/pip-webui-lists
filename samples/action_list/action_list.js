/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appBasicBehaviors.ActionList', []);

    thisModule.controller('ActionListController',
        function($scope) {
            //....
        }
    );

})();
