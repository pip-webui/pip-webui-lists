/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appLists.RefItem', []);

    thisModule.controller('RefItemController',
        function($scope) {
        }
    );

})();
