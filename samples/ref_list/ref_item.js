/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appComponentsStyles.RefItem', []);

    thisModule.controller('RefItemController',
        function($scope) {
        }
    );

})();
